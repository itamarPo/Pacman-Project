#include "Best_Ghost.h"
Direction Best_Ghost::SetMove(const int& maxRow, const int& maxCol, vector<vector<GameBoard>> board, Pacman & pacman)
{
    bool** visited = new bool* [maxRow];
    int
    return Direction();
}
